/**
 * @type {import('@remix-run/dev').AppConfig}
 */
module.exports = {
  appDirectory: "app",
  assetsBuildDirectory: "public/build",
  publicPath: "/build/",
  serverBuildPath: "build/index.js",   // đổi từ server/index.cjs
  serverModuleFormat: "esm",           // dùng ESM
  ignoredRouteFiles: ["**/.*"],

  // Bắt buộc bundle một số dependency để tránh lỗi import ESM/CJS
  serverDependenciesToBundle: [
    /^@shopify\/polaris(\/.*)?$/,
    /^@shopify\/shopify-app-remix(\/.*)?$/
  ],
};
