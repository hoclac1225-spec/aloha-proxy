// src/index.js

// Đây là entry point tạm thời để Rollup build thành công
console.log("Hello from index.js!");

// Export một function đơn giản
export function greet(name) {
  return `Hello, ${name}!`;
}
